import { Meta, Props, Preview, Story } from '@storybook/addon-docs/blocks'
import * as Text from '../../src/components/Text'
import LinkTo from '@storybook/addon-links/react'
import { withKnobs, text, select, boolean } from '@storybook/addon-knobs'
import { fontSizes, fontWeights, fontLineHeights } from '../../src/theme'

<Meta title="Components/Text" component={Text.Span} />

# Text.\*

## Renders a semantically correct text element

```js
import * as Text from './src/components/Text'
```

### Result

<Preview>
	<Story name="Text.[comp]" parameters={{ decorators: [withKnobs] }}>
		<Text.Span
			fontSize={select('fontSize', Object.keys(fontSizes.desktop), 'm2')}
			isMobile={boolean('isMobile', false)}
			fontWeight={select('fontWeight', Object.keys(fontWeights), 'regular')}
			lineHeight={select('lineHeight', Object.keys(fontLineHeights), 'default')}
		>
			{text('Text', 'Intermine Data Browser')}
		</Text.Span>
	</Story>
</Preview>

The following semantic components are exposed from `src/components/Text` to provide
a consistent api.

- `H1`
- `H2`
- `H3`
- `H4`
- `H5`
- `H6`
- `Span`
- `P`
- `Div`

## Playground

Experiment with this story by clicking on the `Canvas` tab

> If the knobs panel is not visible, either press `A` on the keyboard
> or select `show adddons` from the menu dropdown.

## Props

<Props of={Text.Span} />
